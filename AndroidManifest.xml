<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.hughmarshall.metlinktimetables"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="net.hughmarshall.metlinktimetables.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="net.hughmarshall.metlinktimetables.DisplayMessageActivity"
            android:label="@string/title_activity_display_message" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.hughmarshall.metlinktimetables.MainActivity" />
        </activity>
        <activity
            android:name="net.hughmarshall.metlinktimetables.SpinnerActivity"
            android:label="@string/title_activity_spinner" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.hughmarshall.metlinktimetables.MainActivity" />
        </activity>
    </application>

</manifest>